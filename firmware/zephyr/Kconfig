# SPDX-License-Identifier: Apache-2.0
mainmenu "AEL reference firmware"

config AEL_FAULTY_VARIANT
    bool "Build intentionally faulty benchmark mechanisms"
    default n

config AEL_MECHANISM_ID
    int "Compile-time benchmark mechanism identifier"
    range 4 24
    default 4

source "Kconfig.zephyr"
