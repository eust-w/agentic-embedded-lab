# 22 - 802.15.4 interference raises retry, loss, and energy

Mechanism: LR-WPAN interferer transmits.

Causal chain: LR-WPAN interferer transmits -> MAC retries and energy accumulate -> delivery oracle runs.

Fidelity boundary: Channel and radio energy models are scenario-specific.

The variants select different controlled assets; neither experiment contains a direct pass/fail selector. Tool logs and mechanism events are retained in the Evidence Bundle. No result is physical-hardware evidence.
