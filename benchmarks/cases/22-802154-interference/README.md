# 22 - 802.15.4 interference raises retry, loss, and energy

Causal chain: 802.15.4 interference rises -> retry count increases -> loss and energy rise.

Fidelity boundary: Channel and radio energy models are scenario-specific.

`faulty.yaml` must fail its correctness assertion; `fixed.yaml` must pass. Neither result is physical hardware evidence.
